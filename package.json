{
  "scripts": {
    "dev": "npm install && cargo watch -i api.json -i style/output.css -s 'npx tailwindcss -i style/input.css -o style/output.css && rustywind --write . && RUST_LOG=rsmono=debug cargo run --bin server'"
  },
  "devDependencies": {
    "daisyui": "^4.7.2",
    "tailwindcss": "^3.4.1"
  }
}
